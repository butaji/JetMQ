core:
  build: core/
  hostname: core
  privileged: true  
  ports:
    - "1883:1883"
  restart: always

web:
  build: web/
  hostname: web
  ports:
    - "3000:3000"
  restart: always
  mem_limit: 50000000

elk:
  build: elk/
  hostname: elk
  ports:
    - "5601:5601"
    - "5000:5000"
    - "9200:9200"
  restart: always

